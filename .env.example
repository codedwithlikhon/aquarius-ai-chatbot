# Authentication & Session Management
# Generate with `openssl rand -base64 32`
AUTH_SECRET=****
# Required for NextAuth callbacks when running locally
NEXTAUTH_URL=http://localhost:3000/api/auth

# AI Gateway (required outside Vercel where OIDC is injected automatically)
AI_GATEWAY_API_KEY=****

# Storage Providers
# Instructions to create a Vercel Blob Store: https://vercel.com/docs/storage/vercel-blob
BLOB_READ_WRITE_TOKEN=****

# Databases
# Provision Vercel Postgres and copy the pooled connection URL
POSTGRES_URL=****
# Provision Upstash Redis (or compatible) for resumable streams & rate limits
REDIS_URL=****

# Scheduled Jobs
# Protect /api/cron invocations triggered from vercel.json
CRON_SECRET=****

# Testing
# Point Playwright to the dev server URL during e2e runs
PLAYWRIGHT_TEST_BASE_URL=http://127.0.0.1:3000
